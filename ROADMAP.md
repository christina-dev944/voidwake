# Roadmap

Planning lives in **GitHub Issues + the Voidwake project board**, not this file.

- **Issues:** https://github.com/christina-dev944/voidwake/issues
- **Board:** the `Voidwake` project on the `christina-dev944` account (Backlog → Next → In progress → Done)

Labels: `gameplay`, `polish`, `meta`, `tooling`, `bug`, `feedback`, `priority`.

The daily build picks the top `priority` / freshest `feedback` issue, ships it, and closes it via `Fixes #N`. Playtest notes become `feedback` issues.

## Shipped
- v0.1 — core loop (movement, auto-fire, 4 enemy patterns, boss every 5th wave, focus mode, 10-boon level-ups, particles).
- Tooling — Vite + vanilla ES modules, GitHub Actions CI → Pages.
